{
  "name": "@actflow/integrations-privy",
  "version": "0.1.0",
  "private": true,
  "description": "Privy server-wallet provider for ActFlow agents \u2014 implements the agents' IWalletProvider (getAddress/getBalance/pay) backed by Privy server wallets on Arc, with a labeled MOCK mode when no creds are present. No hard-coded secrets; addresses/chain ids config-driven.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "actflow-privy-wallet": "dist/bin/provision-wallet.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test \"dist/tests/*.test.js\"",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@privy-io/node": "0.21.0",
    "viem": "^2.52.2",
    "@actflow/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
