{
  "name": "@actflow/integrations-uniswap",
  "version": "0.1.0",
  "private": true,
  "description": "Uniswap Developer Platform Trading API for ActFlow agents — typed quote/check_approval/swap wrappers + viem execution. No hard-coded API keys, token addresses, or chain IDs.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test \"dist/tests/*.test.js\"",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "viem": "^2.52.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
