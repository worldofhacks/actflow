{
  "name": "@actflow/integrations-x402",
  "version": "0.1.0",
  "private": true,
  "description": "x402-style USDC micropayments for ActFlow agents on Arc — build402Challenge / signPaymentAuthorization (EIP-3009 transferWithAuthorization) / verifyPayment, with a labeled MOCK mode. No hard-coded secrets; addresses/chain ids config-driven (Arc USDC is a cited constant).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test \"dist/tests/*.test.js\"",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@actflow/agents": "workspace:*",
    "viem": "^2.52.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
