{
  "name": "@actflow/agents",
  "version": "0.1.0",
  "private": true,
  "description": "ActFlow agent runtime on Mastra — defineActflowAgent() factory, first-party agents, marketplace/wallet tool interfaces and the task-lifecycle workflow",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test \"dist/tests/*.test.js\"",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@actflow/mcp": "workspace:*",
    "@mastra/core": "^1.42.0",
    "@mastra/mcp": "^1.10.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
